package hW;

import static hW.helper.task;

public class HW_6_2 {
    public static void main(String[] args) {
        task(1);
        for (int i = 10; i >= 0; i--) {
            for (int j = 10; j >= i; j--) {
                System.out.print(j + " ");
            }
            System.out.println();
        }

        task(2);
        for (int i = 0; i < 10; i++) {
            for (int j = 0; j <= i; j++) {
                System.out.print(j + " ");
            }
            System.out.println();
        }
        System.out.println();
        task(22);
        int n = 10; // Количество строк и столбцов

        // Внешний цикл для строк
        for (int i = 0; i < n; i++) {
            // Цикл для пробелов перед числами
            for (int j = 0; j < n - i; j++) {
                System.out.print("  "); // Четыре пробела
            }

            // Цикл для вывода чисел
            for (int k = 0; k <= i; k++) {

                System.out.printf("%2d", k); // Вывод числа с выравниванием по 4 символа
            }

            System.out.println();
//                System.out.printf("%2d", k); // Вывод числа с выравниванием по 4 символа
        }

        System.out.println(); // Переход на новую строку


        task(3);

        for (int i = 0; i < 10; i++) {
            // Вывод отступов перед числами
            for (int j = 0; j < i; j++) {
                System.out.print("  ");
            }

            // Вывод чисел
            for (int j = 0; j < 10 - i; j++) {
                System.out.print(j + " ");
            }

            System.out.println();
        }
        /**
         * for (int i = 0; i < 10; i++): Это внешний цикл, который выполняется 10 раз. Переменная i представляет текущее значение от 0 до 9 включительно.
         *
         * Внутри этого цикла есть первый вложенный цикл: for (int j = 0; j < i; j++). Этот цикл используется для вывода пробелов перед числами. Количество пробелов зависит от значения переменной i. Например, когда i равно 0, ни одного пробела не будет выведено. Когда i равно 1, будет выведен 1 пробел, когда i равно 2, будет выведено 2 пробела, и так далее.
         *
         * После внутреннего цикла пробелов идет еще один вложенный цикл: for (int j = 0; j < 10 - i; j++). Этот цикл используется для вывода чисел от 0 до 10 - i - 1. То есть, когда i равно 0, будут выведены все числа от 0 до 9, когда i равно 1, будут выведены числа от 0 до 8, и так далее.
         *
         * System.out.print(j + " ");: Эта строка выводит текущее значение переменной j, которая представляет число, и добавляет после него пробел.
         *
         * System.out.println();: После завершения цикла вывода чисел для данной строки, эта строка просто выводит символ новой строки, переходя к следующей строке вывода.
         *
         * Таким образом, в результате выполнения этого кода мы получаем рисунок из чисел и пробелов, в котором каждая строка содержит числа, увеличивающиеся слева направо, а количество чисел уменьшается с верхней строки до нижней.
         */
        task(4);


        for (int i = 9; i >= 0; i--) {                       //рисуем пробелы
            for (int j = 9; j > i; j--) {
                System.out.print("  ");
            }
            for (int j = i; j >= 0; j--) {                // погнали с 9 до 0 первую половину
                System.out.print(j + " ");
            }
            for (int j = 1; j <= i; j++) {                      // погнали вторую половину
                System.out.print(j + " ");

            }
            System.out.println();
        }


    }
}


